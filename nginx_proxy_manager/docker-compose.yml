services:
  app:
    image: 'jc21/nginx-proxy-manager:latest'
    restart: unless-stopped
    ports:
      - "${NPM_BIND_IP}:80:80"
      - "${NPM_BIND_IP}:443:443"
      - "${NPM_BIND_IP}:81:81"
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt
